# Test-only dependencies.
#
# starlette 1.x moved its TestClient onto httpx2 and deprecates the httpx path
# (StarletteDeprecationWarning at import). httpx2 is pinned rather than left
# open because of what the previous pin was guarding against: httpx 0.28 removed
# the `httpx.Client(app=...)` signature the older TestClient called, and the
# suite did not fail on that -- it failed to COLLECT. pytest then reports no
# failures and zero tests, which looks green. Pin the client the TestClient
# actually supports so that cannot recur.
httpx2>=2.0.0,<3
pytest>=8.0
